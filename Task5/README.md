Use gradle to generate the compiler (build/libs/LangCompiler.jar will be generated):

`$ gradle jar`

Compile your script:

`$ java -jar ./build/libs/LangCompiler.jar script.sc`

Run it:

`$ java script`
